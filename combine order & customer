SELECT orders.order_number,customers.customer_fname,customers.customer_lname
FROM orders
INNER JOIN customers ON orders.order_id = customers.customer_id;


SELECT orders.order_number,products.product_name
FROM orders
INNER JOIN products ON orders.order_id = products.product_id;
